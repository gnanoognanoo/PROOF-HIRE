{
  "name": "proofhire-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "contracts"
  ],
  "scripts": {
    "dev:web": "npm --workspace=apps/web run dev",
    "build:web": "npm --workspace=apps/web run build",
    "dev:api": "cd apps/api && .\\venv\\Scripts\\uvicorn app.main:app --reload --port 8000"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "es-abstract": "^1.24.2"
  }
}
